
    <!-- Page Content -->
    <div id="page-wrapper">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Blank</h1>
                </div>
                <!-- /.col-lg-12 -->
                <a id= "btn-show" class="btn btn-success btn-circle pull-right">
                     <i class="fa fa-plus"></i>
                </a>
            </div>
            <!-- /.row -->
        </div>
        <!-- /.container-fluid -->
    </div>
    <!-- /#page-wrapper -->
<script>
    $(document).ready(function(){
        $("#btn-show").click(function(){
            var ano = "2017";
            var servico = 'http://localhost:3000/backoffice/dashboard/set_ano?ano=';            
                                                
            Swal.fire({
            title: 'Selecione o Ano Base',
            input: 'select',
            inputOptions: {
                '2017': '2017',
                '2018': '2018'
            },
            showCancelButton: true,
            cancelButtonText: 'Cancelar'
            }).then((result) => {
                if (result.value) {
                 $.getJSON(servico + result.value)
                    .done(
                        Swal.fire({
                            type: 'success',
                            title: 'Ano Base alterado para ' + result.value,
                            showConfirmButton: false,
                            timer: 1500
                        })
                    )
                    .fail(console.log("Erro: " + error.statusText));
                 }
            }); 
        });
    });
</script>


